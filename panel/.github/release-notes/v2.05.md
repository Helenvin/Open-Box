- 出站节点「自动分组」新增故障转移组：勾选后每个国家生成「国家-故转」，主用 = 国家-手动、备用 = 国家-自动，两个组会一并建出来（已存在就跳过）。
- 故障转移的页签可以引用别的分组：主备看的是那个组此刻选中的节点——手动组里挑的节点不通才切到自动组，手动组恢复后自动切回主用。
- 保存时被引用的组改名，页签里的引用跟着迁移，不再静默悬空；引用失效的组 / 节点在保存结果里点名。
- 这个国家还没有节点时，动态组挂的是直连占位，故障转移记「未知」不切换，不会把流量推到兜底拒绝。
- 安装脚本、升级脚本、LuCI 状态页与面板的更新检查统一指向本仓库的发布。
